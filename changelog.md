# V1.1.29

### UI/UX
1. **可视化编排增强**：在“任务管理”>“可视化编排”视图中，新增了任务执行流程的可视化展示组件。
2. **流程图谱**：在配置源与设备映射池之间，插入了动态的“AI智能体处理流程”，包含指令解析、路径生成、用户模拟、执行下发四个关键步骤，直观展示非线性操作逻辑。

# V1.1.28

### UI/UX
1. **文案调整**：将“设备农场”统一更名为“设备管理”，使其表达更加专业和标准化。

# V1.1.27

### UI/UX
1. **品牌重塑**：将应用左上角的Logo标题从“AutoUX ENTERPRISE”更改为“中兴UX智能体”，强化品牌识别度。
2. **设备管理精简**：移除了“设备农场”页面中右上角的“接入设备”按钮，简化界面元素。
3. **场景功能增强**：在“场景库”的“新增场景”弹窗中增加了“场景详细描述”文本域，允许用户输入更详尽的场景说明。

# V1.1.26

### UI/UX
1. **导航栏排序调整**：调整了侧边栏导航菜单的顺序，将“任务管理”提升至第二位（仪表盘下方），更符合“查看概览 -> 创建/管理任务 -> 配置资源”的操作逻辑。

# V1.1.25

### UI/UX
1. **移除冗余设置**：移除了“创建任务”页面中的“执行设置”模块（包括并发执行、省电模式、性能监控等开关），简化了任务创建流程，使用户更专注于核心配置。

# V1.1.24

### UI/UX
1. **设备筛选优化**：将“创建任务”页面中的设备型号筛选器从水平标签（Chips）改为下拉选择框（Dropdown Select），优化了页面空间布局，使其更符合标准表单交互习惯。

# V1.1.23

### Core
1. **设备筛选增强**：在“创建任务”页面的设备选择区域新增了“按型号筛选”功能。系统会自动提取当前可用的设备型号，并生成快捷筛选标签（Chips）。
2. **交互优化**：用户点击特定型号标签后，列表将只展示该型号的设备，配合已实现的单型号选择限制，方便用户快速批量选择同型号设备进行一致性测试。

# V1.1.22

### Core
1. **设备选择逻辑重构**：在“创建任务”流程中，实现了设备型号的一致性校验。现在，选择一个设备型号后，系统会自动限制或提示，不允许混合选择不同型号的设备。点击不同型号的设备会自动清空之前的选择并开始新的型号选择，简化了筛选步骤。

### UI/UX
1. **界面简化**：移除了“目标设备选择”区域的搜索筛选输入框，因为新的交互逻辑（自动按型号过滤）不再依赖显式的文本搜索。

# V1.1.21

### UI/UX
1. **侧边栏优化**：移除了侧边栏中的“测试报告”入口，因报告查看功能已集成至任务管理列表，避免功能冗余和导航混乱。

# V1.1.20

### Core
1. **任务管理与报告整合**：将原本独立的“报告中心”模块深度整合至“任务管理”视图中。现在点击任务列表的“报告”按钮会直接在当前页面展示报告详情，不再跳转至独立页面。

### UI/UX
1. **侧边栏精简**：移除了侧边栏的“报告中心”入口，简化了导航结构。
2. **仪表盘联动**：调整了仪表盘“查看最近报告”按钮的行为，使其跳转至任务列表（任务管理），符合新的交互逻辑。

### Fixed
1. 修复了 `App.tsx` 文件末尾的语法错误。

# V1.1.19

### UI/UX
1. **可视化配置节点样式重构**：将“创建任务”高级模式下的“任务配置”节点从传统的矩形卡片样式改为更现代、紧凑的水平胶囊（Pill Shape）样式。
2. **布局优化**：内部信息（Header、Scenario、Personas）现在采用横向 flex 布局排列，并使用垂直分割线分隔，减少了垂直空间的占用。

# V1.1.18

### UI/UX
1. **可视化布局重构**：将“创建任务”页面中的可视化编排视图由左右结构调整为垂直结构（Top-Bottom）。
   - **顶部**：居中展示任务配置卡片（Source）。
   - **底部**：横向铺满展示映射的设备列表（Target），以网格形式排列。
   - **连接**：增加了垂直方向的虚线连接符，增强了从任务定义到设备执行的视觉流向感。

# V1.1.17

### UI/UX
1. **可视化编排重构**：在创建任务的高级模式中，移除了原有的Start-Action-End流程图节点，改为更直观的“任务配置”卡片。
2. **映射关系可视化**：新增了从左侧“任务配置卡片”（包含场景和画像信息）到右侧“设备列表”的连线映射，直观展示“什么用户在什么场景下操作哪些设备”。
3. **设备缩略图优化**：增强了右侧设备列表的卡片样式，将原有的通用图标替换为模拟的手机屏幕缩略图，点击缩略图可放大预览实时画面。

# V1.1.16

### Feature
1. **可视化编排回归**：在“创建任务”的高级模式中，移除了占位符，完整恢复了可视化拓扑图功能。
2. **场景联动**：拓扑图中的操作节点（Action Node）现在会根据用户在基础配置中选择的“场景”自动更新节点名称。
3. **设备映射可视化**：新增了右侧“映射设备”面板，通过动态 SVG 连线直观展示任务流向设备的映射关系。
4. **屏幕预览增强**：在可视化模式下，支持点击右侧设备卡片唤起模态框，查看设备的实时屏幕预览（模拟画面）。

# V1.1.15

### UI/UX
1. **画像库布局优化**：删除了“用户画像库”网格列表中冗余的“配置新用户画像”虚线占位符卡片，因为页面顶部已提供新建画像按钮，减少了界面干扰。

# V1.1.14

### Feature
1. **创建任务功能恢复**：完整恢复了“创建任务”视图。
   - **基础配置**：包含了任务名称、自然语言描述输入框。
   - **资源选择**：集成了场景预设选择器、用户画像选择器和带搜索功能的设备选择列表。
   - **执行设置**：恢复了并发执行、省电模式、性能监控及重试次数的配置选项。
2. **导航修正**：在侧边栏重新添加了“创建任务”菜单项；修正了仪表盘“创建新任务”按钮的跳转逻辑，现在正确指向任务创建页面而非场景库。

# V1.1.13

### UI/UX
1. **文案回归**：将侧边栏导航菜单及Logo名称恢复为原版中文文案（如“设备农场 Device Farm”恢复为“设备池”），移除了冗余的英文后缀，保持界面语言风格统一。

# V1.1.12

### Feature
1. **报告历史列表**：在“测试报告”模块引入了历史任务列表视图，用户可以浏览所有已完成的测试任务摘要（包含状态、评分、通过率等关键指标）。
2. **报告详情导航**：实现了从任务列表点击跳转至具体报告详情页的交互逻辑，并提供返回列表的导航按钮。
3. **动态报告头**：报告详情页的头部概览区域现在会根据选中的任务动态展示相应的数据（如任务名称、得分、执行时间等）。

# V1.1.11

### UI/UX
1. **界面简化**：移除了“创建任务”页面底部操作栏中冗余的“导入场景”按钮，因为场景选择已集成在主配置区域。

# V1.1.10

### UI/UX
1. **视图分离**：在“创建任务”页面中，彻底分离了“基础配置”与“可视化编排”视图。用户现在通过点击顶部 Tab 切换不同模式，避免了页面内容过长和信息干扰。
2. **交互引导**：优化了可视化编排视图的空状态提示，引导用户先在基础配置中完成设备选择。

# V1.1.9

### Feature
1. **可视化拓扑**：在任务创建流程中新增可视化拓扑视图，支持展示任务与设备的映射关系，并提供设备屏幕预览功能。
2. **设备预览**：拓扑图中的设备节点现支持点击操作，点击后弹出模态框展示该设备的详细状态及模拟屏幕画面。

### UI/UX
1. **动画连接线**：实现了从任务节点到各设备的动态 SVG 连接线效果，增强了视觉反馈。

# V1.1.8

### Feature
1. **数据扩充**：新增了 Z80 Ultra、nubia Air、Z70 Ultra 等最新设备型号，丰富了测试设备池。
2. **交互优化**：确保创建任务页面中的设备筛选功能可以基于新的设备数据进行真实的实时筛选。

# V1.1.7

### Feature
1. **设备筛选**：在创建任务页面的“目标设备选择”模块中增加了搜索框，支持根据设备型号实时筛选列表，方便在大规模设备池中快速定位目标设备。

# V1.1.6

### UI/UX
1. **执行设置调整**：移除了“全称录屏”选项，新增“省电模式”开关，用于配置测试环境的电源状态。

# V1.1.5

### UI/UX
1. **界面简化**：移除了“创建任务”页面右侧的实时预览模块，提供了更宽阔的任务配置空间。

# V1.1.4

### Feature
1. **场景库管理**：实现了独立的“场景库”视图，支持用户增删改查测试场景（包含图标、名称、复杂度、描述）。
2. **场景预设集成**：在“创建任务”页面新增场景选择模块，支持点击场景卡片快速填充任务描述。
3. **导航更新**：侧边栏及顶部导航新增“场景库”入口。

# V1.1.3

### Feature
1. **设备池管理**：实现了独立的“设备池”视图，支持查看所有设备列表、在线状态、电量及活跃时间。
2. **状态仪表盘**：新增设备状态统计卡片（总数、在线、离线、低电量告警）。
3. **导航优化**：将“设备管理”与“测试报告”在导航上彻底分离，侧边栏“设备池”现在可点击并跳转至独立视图。

# V1.1.2

### Feature
1. **用户画像库**：实现了独立的用户画像管理模块。
2. **画像编辑**：支持创建、编辑和删除用户画像。
3. **行为配置**：新增了通过标签系统配置用户行为习惯（如“操作迟缓”、“视力障碍”）的功能。
4. **数据联动**：画像库的修改实时同步至任务创建页面的画像选择列表。

# V1.1.1

### UI/UX
1. 更新了创建任务页面的任务描述输入框占位符文本，提供更详细的引导示例。

# V1.1

### Feature
1. **测试报告中心**：新增独立的测试报告查看页面。
2. **多维度评分**：实现了综合评分环形图及5维雷达图展示（成功率、效率、满意度等）。
3. **画像对比分析**：新增基于表格的用户画像数据对比，支持筛选查看。
4. **智能问题诊断**：实现了可展开的问题卡片，包含虚拟访谈记录、问题定位及AI建议方案。
5. **交互热力图**：新增手机屏幕点击热力图，支持透明度调节。

### UI/UX
1. 报告页面采用卡片式布局，优化数据展示的可读性。
2. 问题诊断模块增加展开/收起动画。
3. 优化了顶部导航栏的选中状态样式。

# V1.0

### Core
1. 完成项目基础架构搭建，采用 React + TypeScript + Tailwind CSS。
2. 集成 Platform Storage SDK，支持本地与平台环境的数据持久化。

### Feature
1. **任务管理**：实现了任务名称、自然语言描述的输入。
2. **用户画像**：支持老年、极客、儿童、商务人士四类画像的多选配置。
3. **设备管理**：实现了设备列表展示与状态（在线/离线）监控。
4. **可视化编排**：提供了基于节点的简易任务流程图编辑器（开始、操作、判断、结束节点）。
5. **执行控制**：支持并发开关、重试次数调节、录屏与性能监控配置。

### UI/UX
1. 采用中兴风格配色（主色 #3498DB，辅色 #2ECC71）。
2. 实现了响应式布局，适配桌面与大屏展示。
3. 增加了丰富的交互动画与悬停效果。
4. 顶部导航与左侧可折叠侧边栏设计。